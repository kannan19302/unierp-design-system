name: unierp_ui_flutter
description: UniERP Design System Flutter Adapter — canonical native mobile and desktop primitives driven by Strata design tokens.
version: 1.1.0
homepage: https://github.com/kannan19302/unierp-design-system
publish_to: "none"

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
