import type { Meta, StoryObj } from '@storybook/react';
import { MultiSelect } from './extended-inputs';

export default { title: 'Components/MultiSelect' } as Meta;
export const Default: StoryObj = { render: () => <MultiSelect /> };
