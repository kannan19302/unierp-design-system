import type { Meta, StoryObj } from '@storybook/react';
import { PercentInput } from './extended-inputs';

export default { title: 'Components/PercentInput' } as Meta;
export const Default: StoryObj = { render: () => <PercentInput /> };
