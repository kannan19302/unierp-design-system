import type { Meta, StoryObj } from '@storybook/react';
import { Steps } from './extended-navigation';

export default { title: 'Components/Steps' } as Meta;
export const Default: StoryObj = { render: () => <Steps /> };
