import type { Meta, StoryObj } from '@storybook/react';
import { Switch } from './extended-inputs';

export default { title: 'Components/Switch' } as Meta;
export const Default: StoryObj = { render: () => <Switch /> };
