import type { Meta, StoryObj } from '@storybook/react';
import { Checkbox } from './extended-inputs';

export default { title: 'Components/Checkbox' } as Meta;
export const Default: StoryObj = { render: () => <Checkbox /> };
