import type { Meta, StoryObj } from '@storybook/react';
import { CommandPalette } from './extended-navigation';

export default { title: 'Components/CommandPalette' } as Meta;
export const Default: StoryObj = { render: () => <CommandPalette /> };
