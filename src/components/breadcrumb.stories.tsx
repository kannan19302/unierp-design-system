import type { Meta, StoryObj } from '@storybook/react';
import { Breadcrumb } from './extended-navigation';

export default { title: 'Components/Breadcrumb' } as Meta;
export const Default: StoryObj = { render: () => <Breadcrumb /> };
