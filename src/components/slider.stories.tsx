import type { Meta, StoryObj } from '@storybook/react';
import { Slider } from './extended-inputs';

export default { title: 'Components/Slider' } as Meta;
export const Default: StoryObj = { render: () => <Slider /> };
