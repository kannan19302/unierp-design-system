import type { Meta, StoryObj } from '@storybook/react';
import { RadioGroup } from './extended-inputs';

export default { title: 'Components/RadioGroup' } as Meta;
export const Default: StoryObj = { render: () => <RadioGroup /> };
