import type { Meta, StoryObj } from '@storybook/react';
import { ImageUpload } from './heavy-inputs';

export default { title: 'Components/ImageUpload' } as Meta;
export const Default: StoryObj = { render: () => <ImageUpload /> };
