import type { Meta, StoryObj } from '@storybook/react';
import { TagInput } from './extended-inputs';

export default { title: 'Components/TagInput' } as Meta;
export const Default: StoryObj = { render: () => <TagInput /> };
