import fs from "fs";
import path from "path";

/**
 * B18 — Cross-platform Design Token Generator
 * Translates TypeScript/CSS design tokens into Dart theme constants for unierp-mobile.
 */
const DESIGN_SYSTEM_DIR = process.cwd();
const MOBILE_TOKENS_DIR = path.resolve(DESIGN_SYSTEM_DIR, "..", "unierp-mobile", "lib", "src", "tokens");

const tokens = {
  colors: {
    primary: "#3b82f6",
    primaryLight: "#e0e7ff",
    secondary: "#64748b",
    success: "#22c55e",
    warning: "#eab308",
    danger: "#ef4444",
    background: "#ffffff",
    bgElevated: "#ffffff",
    bgSunken: "#f8fafc",
    border: "#e2e8f0",
    text: "#0f172a",
    textSecondary: "#475569",
    textMuted: "#94a3b8",
  },
  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
  },
  radius: {
    sm: 4,
    md: 8,
    lg: 12,
    full: 9999,
  },
};

const dartContent = `// GENERATED CODE - DO NOT EDIT BY HAND
// Generated by scripts/generate-cross-platform-tokens.mjs in unierp-design-system

import 'package:flutter/material.dart';

class UniTokens {
  // Colors
  static const Color primary = Color(0xFF3B82F6);
  static const Color primaryLight = Color(0xFFE0E7FF);
  static const Color secondary = Color(0xFF64748B);
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFEAB308);
  static const Color danger = Color(0xFFEF4444);
  static const Color background = Color(0xFFFFFFFF);
  static const Color bgElevated = Color(0xFFFFFFFF);
  static const Color bgSunken = Color(0xFFF8FAFC);
  static const Color border = Color(0xFFE2E8F0);
  static const Color text = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);

  // Spacing
  static const double spaceXs = 4.0;
  static const double spaceSm = 8.0;
  static const double spaceMd = 16.0;
  static const double spaceLg = 24.0;
  static const double spaceXl = 32.0;

  // Radius
  static const double radiusSm = 4.0;
  static const double radiusMd = 8.0;
  static const double radiusLg = 12.0;
}
`;

if (!fs.existsSync(MOBILE_TOKENS_DIR)) {
  fs.mkdirSync(MOBILE_TOKENS_DIR, { recursive: true });
}

const targetFile = path.join(MOBILE_TOKENS_DIR, "tokens.g.dart");
fs.writeFileSync(targetFile, dartContent);

console.log(`✅ Cross-platform Dart tokens generated successfully at ${targetFile}`);
